import { DesignView } from "./DesignView"

/** DesignView reuses createModalOpen to switch between the gallery and new page. */
export function DesignModeEntryView(): React.JSX.Element {
  return <DesignView />
}
