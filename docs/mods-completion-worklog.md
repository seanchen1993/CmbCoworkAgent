# Mods 后续批次工作记录

基于 `codex/mods-v1` / `9e045354`，按用户要求连续完成后续实现和自检。

- 第 2 批：独立受管策略进程、不可被普通插件关闭的部署规则、最终准入/发布、顺序约束、审计和异常核查。
- 第 3 批：命令发现与线程排队、轮次总结、受控文件引用、审计及诊断界面。
- 第 4 批：功能/故障/并发/性能/E2E、生产打包、回归基线清理、发布与回退说明。

每批完成后记录测试与提交。真实外部服务、签名及非本机操作系统的检查须区分实测与不可用环境；不以模拟结果代替。

## 第 2 批

实现独立受管策略进程、部署必需规则、最终准入/发布、稳定顺序约束、原始/最终参数摘要审计、分页、一次性核查及一致性备份。控制记录缺失/无效时保留应用界面并阻止工具执行；恢复步骤见 `mods-operations.md`。

验证：相关 13 文件 90 项通过；补充恢复用例后 manager 11 项通过。Node/Web 类型检查通过；新增及 Mods 相关文件 ESLint 无错误。真实 Electron E2E 9 组通过，正式构建恢复成功。
此次 E2E no-op p95 16.80 ms，审计新增的重复同步写已合并，最终批次需重新测量；含两个 UI 插件与策略的 p95 67.16 ms。关闭功能 A/B p95 -7.66%。原始日志位于忽略目录 `output/mods-validation/batch2/`，这些数字不能代替最终验收。

## 第 3 批

实现命令发现与 `/mod` 输入、共享桌面/IM/定时任务租约的有界队列、取消和重启终态、冷启动普通项目的原生工具上下文、轮次及命令总结、权限控制的文本产物预览/导出。排队请求绑定代码摘要、授权代次和轮次；报告发布前再次核验授权。IM 状态和停止提示识别 Mods 来源。

检视补充：取消不能提前释放尚未结算的实际工具租约；未知操作不重放；审计保存未执行的模块拒绝；检查规则跨发布阶段合并；历史列表读取避免一次打满策略队列，卡片刷新合并在途请求。

验证：15 文件 104 项通过，IM 命令和跨来源租约 standalone 通过，Node/Web 类型检查通过，新增及 Mods 文件 ESLint 无错误。Electron E2E 扩展至 13 组通过，含真实斜杠菜单、排队取消、冷启动命令、文本报告、独立强制策略进程。最近 no-op 1000 次 p95 9.10 ms、待处理请求 0，两个 UI 插件加策略 p95 60.84 ms；最终批次继续验证完整回归和包内运行。原始证据 `output/mods-validation/batch3/`。

## 第 4 批

完成真实 MCP stdio（直接/eager/写后断连）、生产 ASAR 冷启动命令和报告预览/导出验收。
检视发现 MCP 双重调度产生双审计记录，已用先红后绿的回归修复；冷启动共用并发安全的沙箱程序解压。
Windows 日志恢复和测试清理的基线修正独立提交为 `097a7897`。

最终专项 16 文件 107 项、Windows 修正 41 项通过。全量 Vitest 3109 项中 3078 通过、26 失败、5 跳过，
剩余失败逐条与 UAT 对照一致；standalone 81 个脚本中 72 通过、9 个已有失败。Node/Web 类型检查通过，
全仓 ESLint 仍有 96 个既有错误。没有以删除失败用例或修改源码规则获得全绿。

Electron 14 组、正式 ASAR 4 组通过；生成未签名 Windows NSIS 测试包。no-op 1000 次 p95 9.67 ms，
关闭状态实际文件读取 A/B p95 -3.22%，完整示例 UI/策略路径 p95 60.12 ms。
结果、证据、截图、安装包校验和及发布限制汇总于 [最终交付记录](mods-final-delivery-2026-09-16.md)。
