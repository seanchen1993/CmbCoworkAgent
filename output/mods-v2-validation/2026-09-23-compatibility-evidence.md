# Mods v2 兼容声明证据整理 — 2026-09-23

父提交 c4ed6707。只修改兼容表、证据检查和说明，不新增或扩大运行时兼容性。
当前表含重复分类声明，共188项partial、52项adapted、4项unsupported，没有full声明。

先增加检查，复现缺直接测试引用/范围说明。检视后允许已有note/notes两种范围字段，
官方HTTPS链接与本地路径区分，不将网页当本地缺失文件；已有说明不重复添加。
逐项人工核对实际 tests 中的焦点、滚动、agent.offer、压缩提交、usage来源和模型操作边界，
补充16条声明的直接证据或范围说明。所有implementationStatus保持原值。

最终 matrix/evidence **2文件19项通过**，Node/Web类型检查exit0，新增检查ESLint无问题。
底层真实guest/session已经包含在Mods28 **162文件1251项通过**；同一生产实现Electron28
**156 checks exit0**，包含对应能力及关闭、取消、撤权、重启；未为纯元数据改动重复构建。
性能沿用该次实测：关闭读取p95 -0.3110%，noop1000 p95 9.3321ms；正式五轮失败仍未解决。

该检查只验证声明引用的完整性和可查找性，不自动证明同名测试覆盖了全部上游语义。
局部、适配、未支持的字段/时序/错误差异仍以兼容表说明和各能力报告为准。
