# 完成证据扫描的目录预算

仅限制文件数无法限制空目录森林。现在同一证据捕获最多遍历 8,192 个不同路径条目，
超限拒绝生成 PASS 证据，保留原 2,048 文件、24 层深度、单文件 2 MiB / 总 32 MiB 上限。
重叠范围共享预算并去重，避免项目范围、Feature 范围与非 Git 回退反复扫描同一目录。
预算只存在于本次捕获，不跨运行缓存指纹。

先增加真实文件系统失败测试：8,192 个空子目录原实现返回空文件的成功证据，修复后拒绝。
9,000 个重复范围仍产生相同绑定。检视发现默认忽略目录与显式选择目录的区别，补充失败
测试后分开记录已计数和已扫描的路径：先选项目、再显式选 build，仍采集其中产物。

最终证据 / policy manager / freshness 真实 guest 集成及性能纯函数共 5 文件 40 项通过；
扩大回归 170 文件 1,275 项通过。Node 类型和修改文件 ESLint 通过，Web 同轮检查通过。
取消与权限检查保留在每次遍历入口，超限由原完成门禁记录为失败，不由插件宣称通过。

Electron31 **156 项检查 exit0**，普通 out 已恢复。关闭同路径读取 p95 +7.2354% 超过5%阈值，
noop1000 p95 9.0363 ms、pending0，未宣称正式性能门禁通过。大型项目遇到 ENTRY_LIMIT 应缩小检查范围或分拆任务，不可忽略
该错误后直接复用旧 PASS。本项不宣称整个工作区具有文件系统事务隔离。
