# 核心版本两小时桌面验证 — 2026-09-25

冻结功能提交 `971f6d80467031a7f888c33485f5a79fbe68b678`。运行既定 `node tests/run-mods-desktop-soak.mjs`，普通 Electron 构建、八个实际批准的隔离 guest、四个真实 Client 面板；没有测试 IPC bridge。代码、测试驱动和依赖在测量期间保持冻结。

结束后重新计算全部 389 个应用快照文件和 9 个测试驱动文件的 SHA-256，均与 run.json 中运行前的指纹一致，mismatches=[]；核对回执为 `2026-09-25-core-final-soak-fingerprints.json`。

**交互工作量、确认和关闭检查通过；内存稳定性未通过验收。** 不把退出 0 或 qualified=true 解释成全部性能门禁通过。

## 完整结果

北京时间 9 月 24 日 23:56:34 开始实际工作量，9 月 25 日 01:56:41 正常退出，exitCode=0。实际工作量耗时 7206327ms（约 120.11 分钟），status=completed、qualified=true。

- 完整执行 10000 次输入/点击/宿主确认，保留全部 10000 条样本，四个面板各 2500 次。
- 完成 40 次关闭/重新启用/renderer 重载，以及最后一次关闭。41 个关闭观察窗口均无 `CMB Function Mods` 子进程，面板和命令查询均为空；这不是对 legacy Mods 进程的同一断言。
- 282 个强制 GC 观察窗口完整保留。
- 本次未复现旧第 6408 次 ACK 丢失。旧失败保留，不据一次新运行推断已确定原始根因，也不归因于 Pane 焦点清理。

## 退出后的独立持久化核对

应用退出后，使用 Node `DatabaseSync({readOnly:true})` 直接重新打开隔离 profile 的 `data/mods-control.sqlite`，没有实例化会执行恢复写入的控制存储类。

`mods_function_state` 的四个 count 均为 2500；`mods_jobs` 保留 50 条，全部 succeeded。数据库主文件只读核对前后的 SHA-256 相同：`2829815be9d359970c9caa9ae4ab78063ff8861b088906eb4519b0bb4bdd5699`。分析脚本与数据库核对脚本均退出 0。

这证明本次插件状态计数在正常退出后持久保存，不等同于进程硬崩溃、业务验收或 checkpoint 推进测试；这些能力另有独立证据。

## 延迟：全部样本，不删除慢区间

| 测量 | p50 / ms | p95 / ms | 最大 / ms |
| --- | --- | --- | --- |
| 自动化输入调用 | 29.8242 | 41.7216 | 109.3401 |
| 自动化点击到确认 | 398.2664 | 456.8755 | 2112.0270 |
| 可信 input 到第二帧 | 7.9 | 17.4 | 1854.5 |
| 可信 click 到宿主 ACK 的 DOM 更新 | 233.8 | 297.0 | 621.6 |

前两行含 Playwright IPC 和调度，后两行由真实 DOM 事件计时。输入到第二帧的 p95 数值低于原 100ms 目标，但有 330 条（3.3%）超过 100ms：事件 900–1000、1767–1864、1870–2000。保留这些慢样本及未知原因，不能用整体 p95 掩盖局部延迟，也不把点击等待混作输入反馈时间。

在第一段进度更新变慢时，仅做过一次额外 CDP 只读采集和截图；该诊断晚于自然恢复，可能影响邻近测量。详情见[过程记录](2026-09-25-core-soak-observations.md)。除此之外仅轻量文档/状态读取，没有并行构建、测试或大型下载。未调整原工作量、超时、采样和判断阈值。

## 内存：仍有增长，不能标为稳定

每千事件的 live renderer 强制 GC 后堆中位数：

| 事件 | 中位数 / MiB |
| --- | --- |
| 1–1000 | 15.781588 |
| 1001–2000 | 16.477011 |
| 2001–3000 | 16.645512 |
| 3001–4000 | 16.845583 |
| 4001–5000 | 17.015896 |
| 5001–6000 | 17.249744 |
| 6001–7000 | 17.452749 |
| 7001–8000 | 17.670881 |
| 8001–9000 | 17.861897 |
| 9001–10000 | 18.041830 |

每个窗口均有 20 次 live GC 采样。renderer GC 后堆峰值 18.357109MiB；最初 loaded 窗口 13.917110MiB，最后 off 窗口 16.443489MiB。重载产生锯齿波动，后续多个完整窗口的中位数仍持续上升，尚不满足计划中“预热后不呈持续线性增长”的证明要求，留待定位，不能宣称无泄漏。尚未区分应用保留对象、调试/测量对象及运行时缓存的归因。

主 Browser 进程观测到的工作集峰值为 531.085938MiB；这不是主进程强制 GC 后堆，不能与上述 renderer 堆混用或据此认定泄漏。按用户核心优先的方向保留优化项，不改门槛或重复运行以挑选更好结果。当前正式桌面 TTFT +54.6ms 超预算也仍然有效。

## 证据位置

原始目录 `desktop-soak-2026-09-24T15-55-37-149Z-full-b4241798` 中的 run/progress/summary/exit JSON、普通应用和驱动指纹、开始及最终关闭 PNG；日志 `2026-09-24-core-final-soak.log`。

独立分析和只读核对回执：`2026-09-25-core-final-soak-analysis.json`、`2026-09-25-core-final-soak-durable-state.json`，及同日期分析/核对日志。原始数据和诊断脚本保留在忽略目录，仅提交 Markdown 报告。
