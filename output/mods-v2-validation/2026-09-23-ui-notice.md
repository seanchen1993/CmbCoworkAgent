# ui.notice：真实问题生命周期验证（2026-09-23）

基线 ac20adec，host v47；仅 Mods v2 工作树。业务验收与 GitHub Actions 安装包未由本报告证明。

## 行为与代码检视

- 原生 service 捕获工具调用上下文，绑定 acknowledged request、workspace/thread、tool identity、SDK owner；禁止其他插件和已结束请求。无新 IPC，无另建审批路径。
- FunctionSession void SDK 仍跟踪到宿主完成；文字经 middleware、publication，snapshot 固定归属/对话框。32 pending、8行、40ms通知、无空闲定时循环；关闭清理 listener/timer。
- 原生回答、权限、完成证据不由插件说明控制。原问题对话框保留选项、自定义、跳过、提交；关闭后没有门禁或说明轮询。
- 修复真实 Electron 发现的 SDK tool ID 丢失：Session 生成的 ID 传到原 ModsManager identity，不接受插件自行指定ID。另一遗漏是 guest resolve 清单未包含 ToolGroup/AskUserQuestion，改为共用宿主 site 清单。
- adapted 仅覆盖 native request_user_input；OS permission dialog 动态说明、headless 不支持，不宣称 full。

## 失败先行与修复

- ui-notice-red/native-red/session-red/snapshot-red：新模块、SDK、真实服务及发布保护先失败后实现。
- ui-notice-matrix-red：矩阵仍 partial，更新明确 native adaptation 与证据。
- Electron1/2/3未通过：真实callId缺失/SDK调用ID未传入，以及guest组件清单遗漏。
- sdk-id-red、native-identity-red、sites-red在对应修复前复现；修复后实际服务、manager和guest测试通过。
- final-narrow第一轮新增测试错误期待 optional command hook 向顶层抛错；原dispatcher会回退core。改为原.catch观察失败，确认返回REFUSED且没有notice，不改变原错误恢复语义。

## 验证

- Mods21：125文件979测试通过，98.07秒，maxWorkers4。
- 相邻回归：7文件66测试通过，包括原生sandbox、工具调度、MCP重试、user-input transport、真实模型边界。IM remote user-input独立spec通过。
- 最终2文件17测试通过，补两项真实guest拒绝其他插件/已结束dialog。与上述去重为132文件1047测试。
- Node/Web最终typecheck均exit0；作用文件ESLint 0 errors / 146 warnings（已有长文件格式警告保留）；git diff --check通过。
- 聚焦Electron4：5 checks，exit0，普通out已恢复；真实utilityProcess、原生UI、SQLite/授权。说明绑定、undefined移除、原选项提交、迟到拒绝、off、runtime新request、revoke均通过。截图已查看。
- 综合Electron21：120 checks通过，exit0；普通out恢复，out/main/mods-e2e.js不存在。完整结果与截图归档到2026-09-23-electron-21-artifacts。

## 性能与限制

综合E2E关闭对照：500次原生read/每组100次预热，manager absent p95 2.6617ms、disabled 2.7774ms，+4.3468%。noop1000 p95 9.0007ms，pending0；单次观测，不能代表正式5轮达标。此前正式入口5轮预算失败仍未解决，不能以局部通过覆盖；最终全应用性能及2小时soak仍待完成。
全仓历史26项基线失败另有复现记录，不宣称全仓npm test绿。真实Autobiz示例和Actions安装包随后处理。
