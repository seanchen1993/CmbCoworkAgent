# Guest Base64 验证 — 2026-09-24

基线 `d89c8e0d`，宿主修订 `guest-base64-globals-v64`。仅修改 Mods v2 工作树；未修改 UAT、共享依赖或运行本地 NSIS。

## 行为与代码检视

hooks 与独立 Client 提供只读 atob/btoa。纯 guest 算法保存原始字符串 intrinsics、使用空原型解码表，不增加 Node、Buffer、文件、网络或 RPC 权限。维持原 CPU/内存预算，输入限制 512 Ki UTF-16 code units；不保证上限输入一定能在预算内完成。InvalidCharacterError 是 Error.name，尚无 DOMException，矩阵两行从 unsupported 更新为 partial/bounded（全表49 adapted、155 partial、41 unsupported、0 full）。不包含文件 bytes 读取。

## 先失败再实现

- 真实 QuickJS 7 项因缺少 atob/btoa 失败：`2026-09-24-base64-red.log`。
- 旧普通 Electron 首次因缺函数导致 optional command 空文本，JSON 解析失败；补结构化 missing 诊断后 red2 明确返回缺 atob/btoa，未把夹具语法错误当产品 RED。
- 新普通 Electron 首次检查裸 typeof require，看到了 esbuild 局部 shim；改为 globalThis.require/process/__functionHost 后相同普通构建专项5项通过。没有为满足测试开放宿主对象。

## 最终功能验证

- 窄测5文件38项：8 codec、6 codegen、3 global、21 matrix/evidence。覆盖全部256字节、独立 native 向量、padding/ASCII空白/低位、错误、参数转换、只读绑定、原型修改与16 Ki字节往返。
- Node/Web typecheck、helper 类型通过；最终 fixture 修改后 helper 类型再次通过。9文件差量 ESLint 无新增诊断，根 E2E 原88条 warning 保留。
- Mods61：163文件1409项通过，包含全部 classic hooks 与5个 renderer 测试文件。
- 真实 Electron utilityProcess：46项通过；关闭后 runtime/frame/reply/pending/call 均为0。
- 普通 Electron Base64 专项5项通过：实际 SDK 续接、Client 按钮、reload、持久化撤权/重建和关闭对照，截图已检查。
- 完整 Electron：232检查通过，驱动43451退出0并恢复普通 out。结果另存 `2026-09-24-base64-full-electron-result.json`，完整日志 `2026-09-24-base64-full-electron.log`。

## 性能和未完成门禁

生产与驱动冻结，完整回归退出后独占运行短测73290，退出0。目录 `desktop-performance-2026-09-24T08-16-04-456Z-smoke-b36447c4`：TTFT p95关闭125.0ms/开启181.5ms，增加56.5ms；吞吐比1.0028474744；约1秒idle窗口增加1.3164426829百分点。`qualified=false`、`passed=false`；驱动短测退出0不代表性能门禁通过。

未用该结果替换旧v63正式桌面PASS；正式关闭入口仍有2/10失败，旧两小时长稳ACK6408丢失未定位，GitHub Actions真实包/安装验收仍待完成。没有将接口测试或受控模型流称最终业务验收。

下一组 fs.read 参数测试和 Electron helper 仅预备，尚未接线或运行；不纳入本次 Base64 提交。
