# 应用内置项目完成规则验证

应用原 Mods 设置页现在提供四种模式、四种范围、四种检查与修复/时间/token 预算。
规则由宿主按项目和插件持久化；未保存前继承插件行为，保存后插件只读，关闭不被旧插件配置重新开启。
更改规则、替换 runtime、关闭任务会取消正在执行的检查。IPC 复用可信工作区、原设置口令锁与授权。
未增加第二套完成循环，也没有授予额外工具能力。

先失败后实现：初始 manager 方法、IPC handler 和 UI 缺失测试失败；真实 guest 修改规则被拒绝，
原取消信号未中断测试复现后修复。检视追加 file/Feature 缺少目标的 2 个失败测试，宿主保存时拒绝，
不改变旧插件 parser 的兼容语义。原生 Electron 首轮揭示 select 可访问名称错误，修复后第二轮通过。

- 初始联合真实 guest/session、策略、freshness、IPC、SSR：5 文件 42 测试通过。
- 最终 application/SSR：2 文件 11 测试通过（包含新增两个范围校验）。
- 扩大 Mods33：174 文件 1297 测试通过；在最后两个 scope 回归前运行，后者单独验证。
- Node/Web TypeScript 最终均 exit 0。相关 ESLint exit 0，62 个既有大文件格式警告，未全文件格式化。
- 普通 Electron 设置测试：9 项通过，四种模式/范围、检查项/预算、卸载回归、真实进程重启持久化、重新锁定。
- 完整 Electron32：156 项通过，runner exit 0 且普通 out 已恢复。截图已检查，归档 application-policy-electron-artifacts。
- 关闭对照：配置 off 不创建门禁、不执行 guest/model、不生成证据；生产 E2E 原任务关闭对照保持原路径。

Electron32 附带读路径 p95 基线 3.0644 ms、关闭 3.6459 ms，增量 18.976% 超 5%；
no-op 1000 次 p95 9.0235 ms、pending 0、子进程最终 RSS 121380864。
完整回归期间也做了定向测试，不能作为独占性能验收；此前独占五轮性能失败仍未关闭。
正式整应用性能及两小时稳定性另验，不把本报告称为性能或真实 Autobiz 业务验收。
本能力不自动推进 checkpoint，不把插件评审当作宿主测试/业务通过。
