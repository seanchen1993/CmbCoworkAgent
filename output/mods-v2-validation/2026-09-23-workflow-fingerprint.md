# 动态 workflow 证据读取边界

首次六项中五项失败：取消、根 junction、嵌套 junction、过深目录和过量空目录均未拒绝。
改为 opendir 有界遍历，最多 2,048 条目 / 24 层 / 512 文件 / 8 MiB，使用稳定文件句柄
在分配和读取前检查字节上限。拒绝链接和非普通文件，枚举后复核目录成员变化。
原 validator 前后均传入同一取消信号；固定 compiler / validator 版本及业务判定未改。

检视追加“枚举后把目录换成 junction”失败测试，修复后比较打开文件的规范路径和后续路径身份。
新增枚举后增加文件与读中取消关闭句柄的回归。最终 helper 9 项通过；真实固定版本
validator / guest completion 联合 25 项通过（当时 helper 7 项，后补两项独立验证）。
取消、缺失 overlay、错误与超限均不能通过忽略部分内容来生成有效验收指纹。

Node/Web 类型检查与相关 ESLint exit 0。扩大 Mods33 174 文件 1297 测试通过；完整 Electron32
156 项通过，runner exit 0、普通 out 恢复。Electron 是宿主存量回归，不冒称真实业务任务验收。
关闭对照仍走原生路径、不调用 validator；附带 no-op 1000 次 p95 9.0235 ms、pending 0。
读路径基线/关闭 p95 为 3.0644/3.6459 ms（+18.976%，超 5%）；本轮并行定向测试，
不是独占性能验收，正式性能失败仍保留。
合同夹具不是真实业务演示，本修复也不声称冻结整个工作区或消除所有外部写入窗口。
