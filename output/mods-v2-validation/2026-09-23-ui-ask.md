# ui.ask 原生问答验证 — 2026-09-23

基线 e51f99d2；host v45。对照官方2.1.278 SDK，为adapted。

## 实现与代码检视

复用FunctionSession tool.call、ModsManager原授权/lease/generation、原native schema、
createRequestUserInputTool及原renderer。仅SDK适配器强制不延期，无人/跳过/超时不当作
显式用户答案。主Agent原工具schema原样抽取；原可选自动超时配置未改。

先增加missing-module、冷permission query、矩阵红测。审查发现SDK问题原先漏走
classic pre/post，增加Electron红测及LocalSandbox桥接。最初fixture错误使用legacy字段，
已纠正为扁平tool/questions与deny。桥接后聚焦测试等待是未处理最终原生审批框；生产
授权保留，测试显式确认最终参数。再次红测发现拒绝原因丢失及执行状态不准确，现使用
原publication保护拒绝原因，复用beforeModToolExecution记录not_started。

## 验证

- 窄测7文件32项通过，包括真实QuickJS session重入classic、调用者skip、取消迟到回复、
  native validation/wait hooks、拒绝无执行与原因发布。mock单测不作为业务验收。
- Mods19回归113文件931项通过（94.62秒，maxWorkers=4）。包含原工具调度、sandbox、MCP；
  相较上一轮122文件命令筛选不同，未声称测试减少代表功能删除。
- Node/Web typecheck exit0。ESLint最终0错误194警告，多数为既有格式；修复新增测试prefer-const，
  新文件已按项目Prettier格式化。
- focused Electron5：8项exit0，真实utility guest、queued command、tool.call改写、最终审批
  参数、native dialog单选/Other/跳过、classic拒绝在审批和问题前、off/撤权取消，重建可提问。
- 关闭对照：原model turn正常，真实模型协议fixture发起原request_user_input，原问题文字和
  原答案经现有agent loop返回；不是模型服务真实业务验收。截图已查看，原窗口及composer可用。
- 产物：2026-09-23-ui-ask-final-artifacts；每次Electron runner均恢复普通out。
- 综合Electron19：112项通过，归档2026-09-23-electron-19-artifacts。关闭read基线p95
  4.2088ms/off3.9075ms（-7.1588%，各500样本/100预热）；noop1000 p95 9.4959ms，pending0。
  此单轮观察不覆盖此前正式五轮失败，最终整应用性能门禁仍未通过。
- 邻接renderer/native transport及最终nested测试：15文件50项通过（与931项存在重叠，不相加）。

## 兼容与未完成

单选，2–4标签、默认Yes/No，header≤12/question≤500/label≤80；multiSelect显式拒绝，
使用本工程request_user_input。AskUserQuestion render site、ui.notice另行实现。
不增加后台轮询/模型调用；最终性能尚未达标（前版正式五轮记录独立报告，不以局部通过覆盖）。
全仓已归因的26项基线失败仍存在，未声称npm test全绿。不是Autobiz业务验收或安装包验证。
