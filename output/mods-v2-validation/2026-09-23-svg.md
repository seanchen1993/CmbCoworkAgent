# Svg 组件验证

基线 `aacb02be`，2026-09-23，仅 Mods v2 工作树。

- 先增加共享边界及实际编译 guest 测试：原实现拒绝Svg且绘制回退；随后增加构造器、
  受限叶子契约与隔离renderer。夹具最初两次模块路径/导出不合约的失败保留，不算能力红测。
- 边界/实际Pane+Client/旧Client生命周期5文件38测通过；追加旧Pane回调回收、Code、
  原生回退和矩阵7文件24测通过。包含128KiB边界、必填alt、尺寸、未知属性与禁止children。
- Node/Web typecheck通过。首次ESLint报告React将命名props的嵌套字典误作直接props，
  按现有FunctionCode风格显式别名values后复验通过。
- 真实utility process13 exit0，新图形跨guest/Client进程，连续100次缓存快照保持同一
  绘制generation和Client实例；原120帧消息/控件回归继续通过。并非整应用性能资格判定。
- Electron svg1 exit0，3检查：原开关默认关闭；实际安装/授权/命令、Pane静态图、
  Client图像解码、scriptless交互frame、CSS hover；关闭后frame和图像移除，原输入框仍可用。
- 对抗样例包括script、onclick、外部image、foreignObject/iframe、导航链接、CSS import、
  CDATA闭合style注入meta跳转。DOM检查显示危险节点被移除，宿主未写入哨兵变量，
  没有观察到测试外部域请求；sandbox没有任何allow标记。截图已目检。
- 代码检视特别修复XML CDATA在HTML srcdoc中的解析差异：先转为普通文本再序列化，
  不把原始CDATA或插件markup插入应用DOM。源字符串useMemo缓存，节点/深度/长度有界。

原始日志：`2026-09-23-svg-*`、`2026-09-23-electron-svg-1.log`、
`2026-09-23-function-process-13.log`。图像证据：`2026-09-23-svg-artifacts/`。
此为受控组件兼容验证，不是Autobiz业务验收；完整应用性能与最终综合回归仍待执行。
