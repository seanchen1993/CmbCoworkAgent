# 整应用 CPU / streaming 测量记录

本记录仅报告真实桌面链路的受控性能测量，不代表外部模型或 Autobiz 业务验收。

## 新增测量能力

普通应用冻结快照、八个真实已授权 Function guest、四个 Client pane，关闭组验证 pane 和
Mods utility 全部消失。两组各五分钟累计 CPU，五轮各十次流式对照；保持固定
0.5 CPU 百分点 / 40 ms TTFT p95 / 95% 吞吐阈值。纯函数先缺模块失败，再两项通过。
每次模型调用必须恰好一个实际 HTTP 请求，不能用自动压缩、重试或修复掩盖开销。

## 已运行的证据

- smoke `desktop-performance-2026-09-23T14-49-19-730Z-smoke-b3b0014a`：功能 exit0，
  仅两样本/组，永不 qualified。TTFT p95 增量 131.2 ms，吞吐比 0.9953559。
  数值超出 TTFT 阈值，不能声明性能通过。
- 正式 `desktop-performance-2026-09-23T14-51-11-260Z-full-c1115de7`：exit1。
  关闭 CPU 1.804054819、开启 1.883234307 单核百分点，各超过 300000 ms；
  增量 0.079179487，满足该 CPU 条件。
  第一轮各十次流式完成，后续累计实际请求 29、预期 28，按契约失败。
  隔离应用 main.log 证实额外请求 purpose 为 `context-compaction`：32K 测试上下文
  积累触发应用原有压缩。未取得 50/50 样本，整轮不具备完整性能资格。
- 测试上下文随后设为受控服务声明的 1M 容量，仍保留原自动压缩和单请求断言；
  每个流式样本立即写入进度文件，避免失败时丢失当前轮已采集样本。
  此调整不是生产配置修改，也不放宽性能判定。

新版 smoke2：`2026-09-23-desktop-performance-smoke-2-artifacts`，使用同一旧冻结应用，
真实 Electron exit0，6 次物理请求（含两次冷调用）、每组两次样本，qualified=false。
TTFT p95 增量 116.2000 ms；吞吐比 0.995857。
修改 harness ESLint 无错误，相关纯函数两项通过；本轮生产变更扩大回归170文件1275项通过。
正式复测仍待完成。不得将 smoke 或 CPU 单项满足解读为性能验收完成。
