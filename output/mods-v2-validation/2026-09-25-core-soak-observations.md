# 核心桌面长稳过程记录 — 2026-09-25

运行目录 `desktop-soak-2026-09-24T15-55-37-149Z-full-b4241798`，冻结 HEAD `971f6d80`，北京时间 9 月 24 日 23:56:34 开始实际工作量。此页保留运行中的观察；最终工作量/确认/关闭结果与未通过的内存验收详见[完整报告](2026-09-25-core-final-soak.md)。

## 900–1000 次事件之间的绘制延迟

进度文件在 900 次、649704ms 后较长时间未更新。仅进度文件每 100 次才写入，因此单凭该文件停止变化不能判定死锁。只读检查时应用进程仍在运行。

随后自然推进至 1000 次、857525ms（含第四次 off/reload），1100 次、899766ms。GC 窗口仍在事件 950（16:09:05.479Z）、1000（16:10:46.618Z）采集；并非该区间完全没有执行。

当时保留的样本中，事件 931/960/964 的输入到第二帧分别为 1854.5/1842.4/1756.4ms，自动化点击到确认约 2 秒，而可信 click 到宿主 ACK 的 DOM 更新分别为 141.1/138.4/144.8ms。应保留这些慢样本，不能删除后只报正常区间，也不能把它们直接归因为插件处理或操作系统节流。

第四次重载发生于 16:10:46.983Z。之后在 16:11:24.568Z 通过独立 CDP 连接只读采集受控测试面板状态并截图：文档 visible/focused、四 Pane、四按钮未 disabled；当前事件 1077 的可信 input/pointer/click 和 ACK 均已发生，输入到第二帧 2.2ms，click→ACK 296.4ms。诊断晚于恢复，不能称诊断连接使执行恢复。未点击控件、重载页面或改变运行配置。

本轮包含这一次额外 CDP 读取/截图，可能影响邻近测量，最终报告应披露；不重写运行资格、不选择剔除样本。记录为 `2026-09-25-soak-readonly-diagnostic.json`、`2026-09-25-soak-stall.png`，诊断脚本亦保留在忽略目录。不宣称旧第 6408 次 ACK 丢失根因已经定位。

## 一小时中点

completed=5000、cycles=20、elapsed≈60.11 分钟，四个面板各 1250 次确认。20 个 off GC 窗口中的 `CMB Function Mods` 进程均为零；此断言不是对所有 legacy Mods 进程的检查。

前 5000 个原始样本中，330 个 input→second-frame 超过 100ms，最大 1854.5ms。它们继续保留在全量统计中，不按正常/慢区间挑选样本；最终 p95 数值即使达标，也不能删除已经观测到的慢绘制事实。

前四个完整千事件窗口的 live renderer GC 后堆中位数为 15.782、16.477、16.646、16.846 MiB。重载前后存在锯齿波动，整体仍上升，不能据此宣称无持续增长。此时共采集 141 个 GC 窗口，最终仍需统一评估完整趋势。
